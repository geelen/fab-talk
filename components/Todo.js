import React from 'react'
import styled from 'styled-components'

const Outer = styled.div`
  opacity: 0.2;
  font-size: 2em;
  font-variation-settings: "wdth" 60, "wght" 100, "slnt" 12;
`

export default Outer
